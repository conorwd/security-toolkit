[phases.setup]
nixPkgs = ["python310Full", "gcc"]

[phases.install]
cmds = [
  # Install packages with pip from the Python path
  "/nix/store/*/bin/pip install -r requirements.txt --disable-pip-version-check"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT" 